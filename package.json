{
  "name": "tx-realtime-notifs",
  "version": "1.0.0",
  "description": "Realtime TX notifications based on Blockbook",
  "main": "dist/",
  "typings": "dist/",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "build-test": "npm run build && npm run test",
    "version-bump": "npm --no-git-tag-version version patch"
  },
  "keywords": [
    "javascript"
  ],
  "author": "Blockchain Foundry",
  "license": "ISC",
  "dependencies": {
    "@angular/core": "^8.2.12",
    "@types/node": "^12.0.1",
    "rxjs": "^6.5.3",
    "socket.io-client": "^2.3.0",
    "typescript": "^3.4.5"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "jest": "^24.8.0",
    "socket.io": "^2.3.0",
    "ts-jest": "^24.0.2",
    "ts-loader": "^6.0.0"
  },
  "peerDependencies": {
    "axios": ">=0.18.1"
  }
}
